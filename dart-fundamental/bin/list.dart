void main(){

    // list
    List user = [
        'ayub',
        'josep',
        'simon',
    ];

    print(user);
    print(user[2]);

    // map
    Map usere = {
        'name': 'ahmat',
        'age': 19,
        'isSelected': true,
    };
    print(usere);
    print(usere['name']);
    print(usere['age']);
}